# DEPRECATED — Use Canonical Review Gate

This file assumed guest executables would be forced through a QEMU/interpreter engine. That is no longer the reviewed architecture.

User-initiated Linux package-manager behavior remains important, but it is now validated as part of the native PRoot Play architecture and real Internal/Closed Play review.

Use:

- `../v2_0_compliance_roadmap.md` section on guest package managers;
- `../workers/10_release_validation.md` for the package-manager smoke/policy-review test.

Do not describe PRoot as a VM or interpreter.