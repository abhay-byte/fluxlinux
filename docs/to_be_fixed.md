# To Be Fixed

## Termux:X11 Preference Application
**Status**: Flaky / Not Consistently Applying

### Description
Despite multiple implementation attempts, the Termux:X11 preferences (specifically `displayScale`="200" and `displayResolutionMode`="scaled") are not being applied reliably upon XFCE startup. The visual scale remains at 100% (native) in some scenarios.

### Attempts Made
1.  **Command Builder**: Constructing `termux-x11-preference key=value` strings in Kotlin.
2.  **Retro-Loop**: A background retry loop in `start_gui.sh` to apply settings 5 times.
3.  **Argument Passing**: Passing file content as arguments using `$(cat file)`. (Failed due to spaces in parameters).
4.  **Input Redirection**: Using `termux-x11-preference < file` (Reference implementation from `termux-desktop`).
5.  **Full Sync**: Synchronizing all 47 known parameters to prevent default overrides.
6.  **Resolution Mode**: Explicitly setting `displayResolutionMode` to `scaled`.

### Current Implementation
The current codebase uses the **Input Redirection** method (`< file`) with a full 47-parameter list generated by `TermuxX11Preferences.kt`.

### Next Steps (When Revisiting)
- Investigate `termux-x11` server logs for error messages.
- verify if `com.termux.x11` app has permission to write/read the config.
- Test manually running the command inside the Termux environment to isolate script vs. tool issues.
